# Vercel uploads only this package's subtree (no root pnpm lockfile), so the
# build machine resolves with npm. Mirror pnpm's tolerant peer handling:
# @base-ui/react@1 peerOptional date-fns@^4 conflicts with our date-fns@3.
legacy-peer-deps=true
