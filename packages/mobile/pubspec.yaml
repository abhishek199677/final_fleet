name: fleetos_mobile
description: Fleet OS Flutter app (after web launch, TSD §9). Scaffold only until web green.
version: 0.0.1
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  riverpod: ^2.0.0
  drift: ^2.0.0
  dio: ^5.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
